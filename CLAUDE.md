# 🏛 راهنمای هوش مصنوعی و مانیفست معماری IndustrialPlatform

این سند مانیفست اصلی و قوانین تغییرناپذیر مهندسی برای هوش مصنوعی و توسعه‌دهندگان این پروژه است.

---

## ۱. ساختار لایه‌ها در معماری هگزاگونال یکپارچه

سیستم بر بستر **.NET 9 (C# 13)** و در قالب پروژه‌های زیر پیاده‌سازی می‌شود:

### لایه فونداسیون و ابزار مشترک:
- **Shared:**
  - بلوک‌های پایه‌ای سیستم بدون وابستگی بیزینسی.
  - پیاده‌سازی الگوی نتیجه (`Result`, `Result<T>`, `Error`).
  - اینترفیس‌های مشترک (`ISoftDeletable`, `IAuditableEntity`, `IDomainEvent`).
  - کلاس‌های پایه انتیتی‌ها (`BaseEntity`, `AggregateRoot`, `ValueObject`).
  - فرمت‌کننده خروجی API استاندارد (`ApiResponse<T>`) و ابزارهای عمومی (Utilities).

### لایه‌های شش‌گانه معماری هگزاگونال:
1. **Domain (هسته دامنه):**
   - موجودیت‌های کسب‌وکار شهرک‌های صنعتی، اگهی‌ها، دسته‌بندی‌ها و قوانین بیزینس.
   - وابسته فقط به `Shared`.

2. **Application (پورت‌ها و سناریوهای کاربری):**
   - سناریوهای کاربردی (Commands / Queries / Use Cases).
   - پورت‌های ورودی (Inbound Ports) و پورت‌های خروجی (Outbound Ports به عنوان Interface).
   - تعریف مدل‌های DTO، اعتبارسنجی‌ها (FluentValidation) و نگاشت‌ها.
   - وابسته به `Domain` و `Shared`.

3. **Identity (امنیت و احراز هویت):**
   - مدیریت کاربران، نقش‌ها، سطوح دسترسی (RBAC).
   - تولید و اعتبارسنجی JWT (Access Token / Refresh Token).
   - مدیریت فرآیند ورود با شماره موبایل و OTP.
   - پیاده‌سازی پورت‌های امنیتی سیستم.

4. **Persistence (آداپتور پایگاه داده):**
   - پیاده‌سازی دسترسی به داده با EF Core 9 و PostgreSQL.
   - کانتکست دیتابیس (`AppDbContext`)، کانفیگ جداول (Fluent API)، مایگریشن‌ها و ریپازیتوری‌ها.
   - وابسته به `Application` و `Shared`.

5. **Infrastructure (آداپتورهای خدمات خارجی):**
   - پیاده‌سازی پورت‌های خروجی سرویس‌های جانبی: پیامک (ملی‌پیامک)، درگاه پرداخت (زرین‌پال)، کش (Redis) و مدیریت فایل.
   - وابسته به `Application` و `Shared`.

6. **Api (آداپتور راننده و نقطه ورود):**
   - کنترلرها یا Minimal APIها، فیلترها، میدل‌ورهای سراسری، تنظیمات تزریق وابستگی (DI) و نقطه اجرای سیستم (Composition Root).
   - وابسته به `Application`, `Identity` و `Shared`. (هیچ ارتباط مستقیمی با `Persistence` یا `DbContext` ندارد).

---

## ۲. خطوط قرمز و قوانین شکست‌ناپذیر

1. **قانون جهت وابستگی (Dependency Rule):**
   - وابستگی‌ها اکیداً به سمت لایه‌های درونی است. `Domain` از دنیای بیرون کاملاً بی‌خبر است.
   - هیچ کدی در لایه `Api` نباید مستقیماً از لایه دیتابیس (`Persistence`) یا `DbContext` کوئری بزند؛ تمام ارتباطات منحصراً از طریق پورت‌های `Application` و `Identity` صورت می‌گیرد.
2. **الگوی نتیجه (Result Pattern):**
   - برای خطاهای بیزینسی و ولیدیشن‌ها نباید Exception پرتاب شود. تمامی پورت‌ها و Use Caseها باید `Result<T>` برگردانند.
3. **حذف منطقی (Soft Delete):**
   - حذف فیزیکی رکوردها در دیتابیس ممنوع است؛ تمام جداول بیزینسی از `ISoftDeletable` پیروی می‌کنند.
4. **خروجی یکپارچه (API Envelope):**
   - تمام خروجی‌های HTTP به صورت استاندارد در قالب `ApiResponse<T>` به کلاینت فرستاده می‌شوند.
